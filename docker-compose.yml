services:
  homepage:
    image: ghcr.io/ashryverelisa/homepage:latest
    container_name: homepage
    restart: unless-stopped
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"

      - "traefik.http.routers.homepage.entrypoints=websecure"
      - "traefik.http.routers.homepage.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.homepage.tls.certresolver=${CERT_RESOLVER}"
      - "traefik.http.routers.homepage.tls.domains[0].main=${BASE_DOMAIN}"
      - "traefik.http.routers.homepage.tls.domains[0].sans=*.${BASE_DOMAIN}"

      - "traefik.http.services.homepage.loadbalancer.server.scheme=http"
      - "traefik.http.services.homepage.loadbalancer.server.port=80"

networks:
  proxy:
    external: true
